services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: pnl
      POSTGRES_PASSWORD: pnl
      POSTGRES_DB: pnl
    ports:
      - "5432:5432"
    volumes:
      - pnl_db:/var/lib/postgresql/data

volumes:
  pnl_db:

